@model Lawoffice.DTOs.TypeRequest

@{
    ViewData["Title"] = "تعديل نوع الملف";
}

<div class="page-header d-print-none">
    <div class="container-xl">
        <div class="row g-2 align-items-center">
            <div class="col">
                <h2 class="page-title">تعديل نوع الملف</h2>
            </div>
        </div>
    </div>
</div>

<div class="page-body">
    <div class="container-xl">
        <div class="card">
            <div class="row g-0">
        <form method="post">
            @Html.HiddenFor(m => m.id)
            <div class="card-body">
                <h3 class="card-title">تعديل نوع الملف</h3>
                @Html.ValidationSummary(false, "", new { @class = "text-danger" })

                <div class="mb-3">
                    <label class="form-label">اسم النوع</label>
                    @Html.TextBoxFor(m => m.name, new { @class = "form-control", placeholder = "أدخل اسم النوع" })
                    @Html.ValidationMessageFor(m => m.name, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="card-footer text-end">
                <a href="@Url.Action("Index")" class="btn btn-secondary">إلغاء</a>
                <button type="submit" class="btn btn-primary">تحديث</button>
            </div>
        </form>
    </div>
        </div>
    </div>
</div>
